<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.laixer.swabbr">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS" />
    <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <!-- A camera with burst capability is required to use this application -->
    <uses-feature android:name="android.hardware.camera.any" />

    <!-- RequestLegacyExternalStorage is required for our image picker -->
    <application
        android:name="com.laixer.swabbr.App"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Swabbr">

        <!-- Our main entry point -->
        <activity
            android:name="com.laixer.swabbr.presentation.MainActivity"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.Swabbr.Launcher">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.laixer.swabbr.presentation.AuthActivity"
            android:label="@string/login"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.Swabbr.Login"></activity>

        <activity
            android:name="com.laixer.swabbr.presentation.reaction.RecordReactionFragment"
            android:fitsSystemWindows="true"
            android:label="Reaction"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.Swabbr.Recording"></activity>

        <activity
            android:name="com.laixer.swabbr.presentation.streaming.StreamActivity"
            android:fitsSystemWindows="true"
            android:label="Live"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.Swabbr.Recording"></activity>

        <!-- Authenticator service (AccountManager) -->
        <service
            android:name="com.laixer.swabbr.presentation.auth.AuthenticatorService"
            android:process=":authenticator"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>

            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/authenticator" />
        </service>

        <!-- Firebase service -->
        <service
            android:name="com.laixer.swabbr.services.FirebaseService"
            android:exported="false"
            android:stopWithTask="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Meta data -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="firebase_messaging_auto_init_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />

        <!-- FileProvider used to share media with other apps -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
