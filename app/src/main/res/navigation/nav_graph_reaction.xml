<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_reaction"
    app:startDestination="@id/reaction_dest">
    <argument
        android:name="vlogId"
        app:argType="string" />

    <fragment
        android:id="@+id/reaction_dest"
        android:name="com.laixer.swabbr.presentation.reaction.ReactionFragment"
        tools:layout="@layout/fragment_livestream">
        <argument
            android:name="vlogId"
            app:argType="string" />
    </fragment>

    <activity
        android:id="@+id/mainActivity"
        android:name="com.laixer.swabbr.presentation.MainActivity" />
</navigation>
